import screenSize from './ContainerSize'
import boxShadow from './BoxShadow'
import borderRadius from './Borders'

export default { screenSize, boxShadow, borderRadius }

/* These are common and helpful css styles that i use often. Most of these are based off Tailwindcss Utility Styles (https://tailwindcss.com/). Which i would highly suggest if you are not useing style components */
